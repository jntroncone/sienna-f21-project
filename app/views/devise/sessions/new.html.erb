<body class="<%= cookies[:theme] %>">

  <% if cookies[:theme] == "light" %>
    <%= link_to "Toggle Dark Theme", root_path(theme: "dark"), class:'like-button text-decoration-none p-2' %>
  <% else %>
    <%= link_to "Toggle Light Theme", root_path(theme: "light"), class:'like-button text-decoration-none p-2' %>
  <% end %>
  
  <%= yield %>

  <% if cookies[:theme] == "light" %>
    <div style="color:black;">
    <body style="background-color:#B0C19D;">
      <div class="container h-200">
        <div class="row h-200">
          <div class="col-sm-10 col-md-8 col-lg-6 mx-auto d-table h-200">
            <div class="d-table-cell align-middle">
              <div class="text-center mt-4">
                <h1 class="h1">Welcome!</h1>
                </div>
                <div class="card bg-light mb-3">
                  <div class="card-body"style="background-color:#c7b7c6;">
                    <div class="m-m-5">
                      <div class="text-center">
                        <img src="https://cdn.discordapp.com/attachments/899742947472453632/911038158790090802/sleepy_cat_logo_design_4x.png" alt="Sleepy Cat" class="img-fluid rounded-circle" width="132" height="132">
                      </div>
                      <div class = "text-center mt-4">
                        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                          <div class="field">
                          <%= f.label :email %><br />
                          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "bg-white" %>
                          </div>

                        <div class="field">
                          <%= f.label :password %><br />
                          <%= f.password_field :password, autocomplete: "current-password" , class: "bg-white" %>
                        </div>

                        <% if devise_mapping.rememberable? %>
                          <div class="field">
                            <%= f.check_box :remember_me %>
                            <%= f.label :remember_me %>
                          </div>
                        <% end %>

                        <div class="actions login">
                          <%= f.submit "Log in", class: "btn-primary" %>
                        </div>
                        <%= render "devise/shared/onlysignup" %>
                        <% end %>
                        </form>
                      </div>
                    </div>
                  </div>
                </div>

              </div>
            </div>
          </div>
        </div>
      </div>
    </body>
  </div>
  <% else %>
    <div style="color:white"> 
    <body style="background-color:#121212;">
    <div class="container h-200">
      <div class="row h-200">
        <div class="col-sm-10 col-md-8 col-lg-6 mx-auto d-table h-200">
          <div class="d-table-cell align-middle">
            <div class="text-center mt-4">
              <h1 class="h1">Welcome!</h1>
              </div>
              <div class="card bg-light mb-3">
                <div class="card-body"style="background-color:#ad89e8;">
                  <div class="m-m-5">
                    <div class="text-center">
                      <img src="https://cdn.discordapp.com/attachments/899742947472453632/911038158790090802/sleepy_cat_logo_design_4x.png" alt="Sleepy Cat" class="img-fluid rounded-circle" width="132" height="132">
                    </div>
                    <div class = "text-center mt-4">
                      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                        <div class="field">
                        <%= f.label :email %><br />
                        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "bg-dark" %>
                        </div>

                      <div class="field">
                        <%= f.label :password %><br />
                        <%= f.password_field :password, autocomplete: "current-password" , class: "bg-dark" %>
                      </div>

                      <% if devise_mapping.rememberable? %>
                        <div class="field">
                          <%= f.check_box :remember_me %>
                          <%= f.label :remember_me %>
                        </div>
                      <% end %>

                      <div class="actions login">
                        <%= f.submit "Log in", class: "btn-primary" %>
                      </div>
                      <%= render "devise/shared/onlysignup" %>
                      <% end %>
                      </form>
                    </div>
                  </div>
                </div>
              </div>

            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
  </body>
  </div>
<% end %>