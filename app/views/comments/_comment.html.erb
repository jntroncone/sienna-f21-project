<% unless !comment.persisted? %>
<div class="card">
  <div class="card-body">
    <%= comment.body %>
     </p>
  <p>
    <%= link_to 'Delete', [comment.post, comment],
               :method => :delete,
               data: { confirm: 'Are you sure?' }
                %>
  </p>
  </div>
</div>
<% end %>
